import { createFileRoute } from "@tanstack/react-router";
import SecureCloud from "@/pages/solutions/SecureCloud";

export const Route = createFileRoute("/solutions/secure-cloud")({ component: SecureCloud });